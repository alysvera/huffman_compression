# huffman_compression

Developed a file compression and decompression tool using Huffman coding to reduce file sizes through lossless data compression. Implemented Huffman tree encoding/decoding and custom I/O streams with bitwise buffering to efficiently process compressed data, achieving approximately a 43% reduction in file size.
